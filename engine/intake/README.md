# Engine · Intake

Turns normalized source events into execution objects.

Email adapters produce events. Intake never imports Gmail/Outlook.
Document storage is injected via ports so the Engine does not depend on adapters.
