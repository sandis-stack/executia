# @executia/core-ai

Provider-independent EXECUTIA Core AI — constitution, memory, validation, ResponseGate.

```bash
cp .env.example .env
npx prisma migrate dev
npm test
```

Public entry: `executeCoreRequest()` — user-facing delivery **only** via ResponseGate.

See `docs/EXECUTIA_CORE_AI.md`.
